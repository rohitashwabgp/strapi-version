'use strict';

/**
 * version-test-dynamic-zone router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::version-test-dynamic-zone.version-test-dynamic-zone');
