'use strict';

/**
 *  version-test-dynamic-zone controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::version-test-dynamic-zone.version-test-dynamic-zone');
