'use strict';

/**
 * version-test-dynamic-zone service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::version-test-dynamic-zone.version-test-dynamic-zone');
