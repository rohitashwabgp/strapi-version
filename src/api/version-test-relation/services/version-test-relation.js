'use strict';

/**
 * version-test-relation service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::version-test-relation.version-test-relation');
