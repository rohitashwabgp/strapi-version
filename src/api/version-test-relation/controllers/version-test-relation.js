'use strict';

/**
 *  version-test-relation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::version-test-relation.version-test-relation');
